# Night log

Unattended work while the user sleeps (2026-09-15). Rules and backlog: `.claude/skills/night-shift/SKILL.md`.
Read top to bottom; each entry is one committed increment. "Ask" items are taste questions I decided conservatively.

| Time | Item | Result | PixelLab spend |
|---|---|---|---|
| 00:xx | 1 · Boss presentation | Intro: Wren held 3.4s, steps clear, roots seal the south doorway, camera leans in 1.5x on the Treant while its core flashes amber x3, quake, BOSS banner (now lower third so it never covers the Treant), camera back, bar appears, first attack delayed. Death: white-out, roots let go, shard rises. Phase 2 tell: bark darkens, faster sway, "IT DIGS IN DEEPER". Fixed a real bug: the Treant never flagged itself dead (could be hit after death). Hotbar scale fixed on tiny windows (`--sh` capped at `--s`). Scripted run: intro/fight/death states, 0 HUD overlaps at 2x. | 0 |
| 01:xx | 2 · Wren hurt + death clips | PixelLab templates taking-punch (hurt, 6f, 8 dirs — the template forced 8) and falling-back-death (death, 7f, 4 dirs); normalised to 68px. Getting hit now plays a 260ms flinch facing the attacker (cancels a swing in progress); dying plays the collapse and holds the last frame before the death screen. Bonus: last heart beats when you're at 1 heart. Review: docs/sprite-review/wren-hurt-sheet.png, wren-death-sheet.png. Scripted: hurt/death states verified. | 12 |
| 01:xx | 3 · Enemy frames | PixelLab animate_object (v3) on the existing objects: slime hop loop (6f) + burst (6f, played under the fade — alone it reads as a wobble), forest sprite wing-flutter loop (6f), mushroom spore clip (8f over the whole telegraph+puff). Frames land in `public/assets/sprites/props/anim/<clip>/`; `tools/fetch_objects.py --anim` fetches + makes review strips. Review: docs/sprite-review/m2-enemy-anims.png. Tween tells (squash, glints, shudder) kept on top. Scripted: all four clips play in the right states. | 4 |
| 01:xx | 4 · Room transitions | Locked and boss doors now swing open (PixelLab 6-frame clips: chain drops, leaf swings inward; roots writhe aside for the boss door) and leave an empty stone archway behind; archways also stand over unlocked top doorways on later visits. Wren plays her walk clip while the camera scrolls instead of sliding. Torches cast a warm breathing halo (additive, generated texture). Review: docs/sprite-review/m2-doors.png. Scripted: unlock → clip → arch, scroll → walk → idle. | 7 |
| 01:xx | 5 · Story beats | New game now opens on three plates over the title world (the Ember, the Splitting, Wren sets out — any key advances, Esc skips). Taking the shard leads to a "Cleansed" screen: shards 1/3, gold, playtime, "Keep exploring" (stays where you are) or "Return to title". Three more lore signs (Slime Warren, Mossback's Den, Forgotten Larder). Scripted: intro → game, boss → shard → complete → keep exploring. Ask: the Sunken Crypt line on the complete screen is a placeholder until the hub exists. | 0 |
| 01:xx | 6 · Title screen life | The world stays alive behind the title/intro: camera zoomed 1.15x drifting slowly over the antechamber, embers rising (additive particles), Wren waiting at the spawn. Quit-to-title resets into that attract mode (nothing can hurt her behind the menu). Continue card gained shard tally + dungeon/room line. | 0 |
| 08:xx | User feedback (morning) | Strike lesson now only fires when something hostile spawns (not the mushroom in the cellar) and situational lessons (strike/potion/bomb) drop when you leave the room; wording is "Aim with the mouse, click to strike". Potion is locked until the Slime Warren and bomb until the Whispering Gallery (padlock on the slot, using them earlier gives a hint); first hit taken with a potion unlocked teaches "1 · Drink a potion to heal". Minimap redrawn as pixel tiles (`tools/draw_minimap.py`) with doorway links between rooms you've seen. Room lore is now narrated on entry as a caption (no hold) — signs stay bumpable. | 0 |
| 09:xx | 7 · Feel pass | Hearts pop when healed, gold/key counters flash on change, red vignette pulses at one heart, screen-shake setting (Full/Low/Off) in the pause menu, persisted in `emberfall.settings` separately from the save and routed through one `scene.shake()` helper. | 0 |
| 09:xx | 8 · Minimal audio | `src/game/audio.ts`: a WebAudio synth (oscillators + filtered noise, no files) with 29 placeholder sounds — sword hit / bark clang / hurt / dash / pickups / key / chest / doors / boomerang whoosh + catch / stun / bomb / potion / slime & sprite deaths / spores / block / plate / crystal / boss roar, phase, victory / death / UI / lesson chime / wall crack. Wired to every event; volume (Off…100%) in the pause menu next to screen shake; starts on the first click/key as browsers require. Ask: these are stand-ins — worth replacing with real samples later, but they already carry the hits. | 0 |
| 09:xx | User: boss plate → bar | The boss name now lives in the stone plate: it lands mid-screen on the intro (BOSS · ELDER TREANT · Warden of the Hollow), then the same plate rides up into the wall band, sheds its subtitle and grows the health bar inside. One element, one transition (0.7s). | 0 |
| 09:xx | User: boss bar looks generic | Every boss now gets its own bar frame (`BOSS_BARS` in HUD.tsx: frame PNG + fill channel + colours). Elder Treant: gnarled roots with an ember-shard end cap and leaves, sap-orange fill behind a transparent channel (PixelLab, 192x32). The stone name plate still lands mid-screen on the intro, then rises and hands over to the bespoke bar. Bosses without a frame fall back to the stone bar. Preview: docs/sprite-review/bossbar-treant-ingame.png. | 6 |
| 10:xx | 9a · Hub design + assets | Emberfall town drafted as a 40x30 ASCII map (`tools/make_town.py` → `src/game/data/emberfall-town.json`, overview `docs/mockup/town-map.png`): central plaza with the unlit Ember plinth, elder's hall north, forge NE, apothecary NW, save shrine south, Whisperwood gate east, two sealed gates (crypt west, cinder SW), tree border. PixelLab: town tileset (flagstone chained from the dungeon floor → olive grass; took 3 tries — first came out as bright cobbles, second flat green), forge / apothecary / elder's hall (96px), shrine, plinth, tree, gate, and three NPCs styled on Wren (Orrin the blacksmith, Maren the apothecary, Elder Tam). Review: docs/sprite-review/m3-town-assets.png, town-tileset.png. Build is the next cycle. | ~62 |
| 10:xx | 9b · Hub built | `HubScene`: the town is a free-scrolling 40x30 map (camera follows Wren) built by the same autotiler — flagstone paths, grass, tree border. Buildings, plinth (an ember flickers on it once you own a shard), shrine (bump: heal + "keeps your progress"), sealed gates (bump: a line about why not yet), Whisperwood gate (walk through: fade, dungeon entrance). The dungeon entrance gained a south doorway back to town. NPCs are solid and talk on bump (Elder Tam points east; his line changes once the shard is home). New games start in town after the prologue; saves remember `place` (save v4). `Player` now talks to a `PlayerHost` interface so both scenes can host her; the minimap hides in town. Scripted: elder/shrine/gates, town → dungeon → town, continue into either place, death → respawn at the entrance. | 0 |
| 10:xx | 10 · Shop + upgrades | Bump Maren or Orrin to open their stall (pixel panel: card per item with price and the delta, greyed when you can't afford / can't carry more / already own it, Esc leaves). Maren: potions 20g (max 5), bombs 15g (max 5), one heart container 120g. Orrin: Tempered Sword 80g (2 dmg), Ember-forged 200g (3 dmg, needs the first). Sword tier drives strike damage and the bag hint; saved (v5). Skipped armour tiers — with half-heart damage there's no honest number to show; noted for later. | 0 |

Backlog complete (1–10). Waiting on the user for the next direction: Sunken Crypt, or another pass on what's here.
| 11:xx | Hub visual QA | First captures (docs/sprite-review/hub-qa.png) showed a golf course: 40x30 of flat grass around five props. Rebuilt the town at 32x24 with paths that reach every door, lanterns (with halos) on the plaza corners, a well, crates by the forge, stones, ruined wall bits, tree clumps; sealed gates are now visibly barred (roots on the marsh gate, fallen blocks on the mountain lane) and solid. After: docs/sprite-review/hub-qa-2.png. Known: the "bush" prop reads as a stone (kept as stones); the tree border reads as a hedge. | 25 |
| 11:xx | User: grass/path edge + house size | Tileset v5: soft olive lawn with speckles and a kerbed, organic path edge (round transition, chained from the dungeon flagstone; v4 with the hedge grass kept as `town-kerb.*`). Houses regenerated at 128px (4x4 tiles) with more detail; map re-planned so every door meets its path. Review: docs/sprite-review/hub-qa-3.png, m3-houses-xl.png. | ~26 |
| 13:xx | Sunken Crypt (dungeon 2) | Map authored in `tools/make_crypt.py` (10 rooms, 4x3): drowned halls that drain when you stand on a rune plate, a cistern you cross by grapple hook, ossuary/bat roost/captain's vault fights, the Bone Knight's hall. DungeonScene is now generic over `src/game/data/dungeons.ts` (tileset, boss, sign title, banner text, unlock rooms per dungeon; the Hub's west gate opens once `shard:whisperwood` is set). New: water/pit tiles (static fence, blue slimes swim), anchors + `Grapple` tool (RMB, Q swaps with the boomerang; reels Wren past the post, reels foes in), `Skeleton` (+captain), bats (ForestSprite variant), `BoneKnight` (tower shield blocks the front, hook tears it off for a window, charges, phase 2). PixelLab: crypt tileset (cooled in `tools/draw_water.py`), skeleton/bat/knight/sarcophagus/bones/anchor/hook/pit; water tile drawn in Python after PixelLab refused a flat fill. Verified end to end with scripted runs (`__start` in tools/playtest.js; `__run` now skews Date.now so tweens follow virtual time). | ~70 |
| 14:xx | User: overlaps + west gate | The v5 kerb read as a raised ledge (its "enhanced" prompt said "mid-height wall"): regenerated as a flat, same-level grass edge (`enhance:false`, v5 kept as town-kerb-v5). Tree border walls thickened so Wren isn't drawn tangled in the canopy. World tags hide while a dialogue is open (only HUD overlap the checker found). West gate is now side-on (`tools/make_side_gate.py` composes two pillars + sign from the front gate; PixelLab kept returning it front-facing). Bone Knight has his own bar frame (skull, femurs, chain, shield; teal fill). | ~9 |
| 15:xx | Backlog 1–5 after the crypt | **Knock-through fixed**: a sealed boss room clamps Wren inside (the Bone Knight's charge could shove her body through the seal zones in one physics step). Crypt polish: drain / bones / squeak / hook synth sounds, skeleton swing arcs, charge dust, "The Captain waits" plate line. Town: real bush sprite (PixelLab, the old one is now `stone`), per-tree flip/scale/shade so the border isn't a hedge, textures aren't re-fetched on scene restarts (the "Failed to process file" errors that remain are Vite's stale public-dir listing for files added while the dev server runs — restart it; production is unaffected). **Cinder Depths** drafted only: `docs/mockup/cinder-map.png` + `docs/design/cinder-depths.md` (lava, breathing vents, braziers, Fire Rod, Cinder Golem) with three questions for the user. **Armour** at Orrin: leather (every 3rd hit glances) and iron (every 2nd), a clang + "Armour held" tag, shown in the bag. **Ambience**: a quiet bed per place (town breeze + birds, wood wind + creaks, crypt draught + drips) scaled by the SFX setting; hit/door/potion re-voiced. | ~5 |
| 16:xx | Regression check + progression | Re-ran Whisperwood end to end after the generalisation: the Treant had lost `isBoss` (bar didn't update, boomerang didn't stop on it) — fixed, plus `blocksNow` and a rooted `grappled()`. Plinth now burns one flame per shard (amber, then blue) and Elder Tam's line follows the shard count (points at the south road after the crypt). Rule added to `emberfall-playtest`: both boss fights after any enemy/scene/store change. | 0 |
| 17:xx | User: character/monster sounds + music | `src/game/music.ts`: a procedural chiptune sequencer (square lead with vibrato, triangle bass, detuned-saw pad, sine kick / noise snare & hat) with five tracks — title (A minor, slow), town (C major lilt), explore (D minor, sparse), fight (E minor, driving), boss (C minor, heavy). Switches wait for the bar line (fight/boss: the next beat) with a dip so key changes don't smear; dialogue/shop/pause/complete duck it; death stops it; Music level in the pause menu (settings.music). Hooks: town on the hub, explore/fight per room (mushrooms alone don't count), explore again on clear, boss at the roar, victory sting then explore. Wren: footsteps (stone / turf, quicker when sprinting), sword swing + an effort breath every third swing. Monsters: slime tell, skeleton rattle, bat flap, knight steps + growl, treant creak, per-type hurt sounds. Offline previews: `renderPreview()` → `docs/audio-review/*.wav` (+ spectrogram sheet) via the snapshot sink. | 0 |
| 18:xx | User: shield down, hook-dodge attack, girl voice | Bone Knight: hooking him now swaps to a shieldless sprite (PixelLab, same character) and the shield tumbles onto the flagstones (`shield-ground`) until he recovers and it slides back. New attack **Quake**: every fifth attack (phase 2: third) he raises the sword for 1.7 s — status "QUAKE - HOOK A POST", the four anchor posts now in his hall pulse, a one-time hint tag — then drives it into the floor: two hearts to anyone on the stone; safe mid-reel or beside a post ("Safe on the post/hook"). Body made taller so a chest-level hook connects. Wren has a voice: a formant synth (two detuned saws through vowel bandpasses) for hurt "ah!", dash "hup", effort, "hm?" on lessons, a battle cry when a boss opens up, a hum after a potion, and a falling "aah…" on death. Side doorways draw an edge-on locked door (`door-locked-edge`, composed) instead of the front door lying on the floor. Boss status line is clamped clear of the bar and minimap. Previews: `renderSfxPreview()` → docs/audio-review/sfx-*.wav. | ~10 |
| 19:xx | User: quest guide + story, voice | **Main quest** (`src/game/quests.ts`, 15 steps from "The Ember Cracked" to "The Mountain Road"): progress is derived from flags/items/place, so old saves land on the right step. HUD tracker top-left (title · n/15 · J, objective, direction + distance), a **journal** (J) with done/current/next steps and the story line, a NEW OBJECTIVE plate + narrated story when a step begins, a pulsing marker on the objective's room in the minimap. **GPS** (`src/game/guide.ts`): a gold chevron beside Wren and a dotted trail toward the objective — inside a room straight at the thing (chest, plate, boss, shard), across rooms via BFS over open doorways (`Dungeon.exits/nextHop`; cracked walls and the crow-flies fallback handled), out of the dungeon via the entrance, in town toward the NPC or gate. Elder talks set `talked:elder[:n]`; the Boss Key is spent on the boss door. **Voice**: `tools/make_voice.py` renders Wren's cues and four spoken lines with a free neural TTS (edge-tts; default en-GB-MaisieNeural) into `public/assets/voice/wren/*.mp3`; the game plays clips with rate jitter and falls back to the formant synth per cue; `speak()` for lines (meeting Tam, taking a shard, low hearts, boss roar). Level: makeup + compressor on the master. Potion lesson clears itself at full hearts. | 0 |
| 20:xx | User: perfect the hub + two dungeons (pass 1) | Room contact sheets (`docs/sprite-review/qa-whisperwood.png`, `qa-crypt.png`) showed bare floors. Floor dressing per dungeon (PixelLab, 8 decor sprites: leaves, grass tuft, mushrooms, puddle / moss, rubble, dark puddle, skull + candles), scattered on empty tiles by a room-seeded RNG (`scatterDecor`: never on doorway lanes, never on a used tile, spaced, flats at 82% alpha). Crypt floor cooled harder so it reads as damp stone next to Whisperwood's green. | ~40 |
| 21:xx | Cinder Depths (dungeon 3) built | Rooms from `tools/make_cinder.py` → `src/game/data/cinder-depths.json`; registered (`tileset cinder`, gate south, boss cindergolem, tool firerod). New tile kinds: **lava** (fence like water; a plate "drain" crusts it to slag), **vents** (shared 3 s breath: rest, glow, flame column + hazard), **braziers** (rod lights them; all lit → solve), **thorns** (a doorway barricade the rod burns; flag persists). **Fire Rod** on RMB (Q cycles three tools): an ember bolt with a spark trail; heats cinderlings (guard drops), lights, burns, singes for 1. Enemies: magma slime (leaves fire where it lands, crosses lava), fire bat, cinderling (iron shield turns steel from the front until heated). **Cinder Golem**: turns slowly to face her, stomps (expanding ring, 2 hearts if you're on it), hurls warned magma splashes that leave fire; steel does nothing until a rod bolt in its BACK overheats it (4 s window, 3 s in phase 2). **Finale**: three shards to Tam → camera on the plinth, the flame returns, "The Ember Is Whole" plate and ending screen. PixelLab: tileset (floor warmed from slate to ash in `tools/draw_lava.py`), cinderling, golem, brazier, thorns, vent, fire rod icon, golem bar frame; lava/slag/magma slime/fire bat drawn or recoloured in Python. Verified end to end by script (vents hurt, plate, smelter clear → rod chest, 4 braziers → boss key, thorns burn, lava cools, golem overheats × 3 → shard → complete → town finale). Fixes on the way: quest index = furthest done step (old saves land right), journal key J collided with the J attack key → **M**. | ~45 |
| 22:xx | Quest guidance QA + critique | Researched wayfinding (Level Design Book certainty ladder; Game Developer quest-UX article; Genshin's beam-only-when-far). Critique of our system → fixes: never questless (open epilogue step), completion feedback (old objective strikes through + chime; next plate waits 2.4 s), progress counts (`questNote`: braziers, crystals), journal keeps every story line, the chevron only shows when the goal isn't in plain sight (>6 tiles in-room, other rooms) and can be switched to "Tracker only" in pause, "where" reads as words (right here / this room / 2 rooms away). Captured as the `quest-guidance` skill with the QA checklist. | 0 |
| 23:xx | User feedback round | **Guide**: the chevron + trail is gone; a small gold square (same mark as the minimap's) hovers over the objective when it's on screen and sits at the edge of the view in its direction when it isn't. **Tracker** moved under the minimap (top-right, right-aligned, 88% opacity). **Side doors**: the front door art rotated to face into the room (`door-locked-w/e`, `door-boss-w/e`) — the edge-on composite is retired. **Crypt loop**: the Drowned Hall's north door is open (the plate drains the way), so the ossuary key opens the Crossing's west door → Captain's Vault → hook, as intended; the cistern's pool now reaches both walls and every water/lava/pit fence grows 20 px into an adjacent wall, so the walkable sliver under a wall face can't bypass a pool. **Bombs** are a surprise again: no lesson tag by cracked walls. **Bosses earn their window**: the Treant's core only opens for ~2.4 s after a slam (glow + "THE CORE SHOWS - RING IT"; otherwise "The bark is shut"); the Bone Knight's shield can only be hooked in the ~1.6–2.2 s after a swing or charge ("HIS ARM DROPS - HOOK THE SHIELD"; otherwise the hook glances off). Quake tell slowed to 2.5 s (1.9 s in phase 2). | 0 |
| 00:xx | Cycle 1 (five beats) — regression playthrough | **Brainstorm**: regression script / gamepad / side quests / room polish / balance / world map. **Plan**: `tools/regression.js` — a fresh save played to the finale by script (real fights, chests, tools, boss windows; `goto` only between rooms), one assertion per beat, sectioned so a crash still yields a report. **Reality check**: the browser tool times out at 45 s, so the run is started in the background and polled; pickups spawn on a delay (poll); shield-bearers must be cut from behind; tier-1 sword makes the golem a slog (the script buys Orrin's sword — a shop beat for free). **Action**: first runs found two real bugs — the south gate was walled off by the thicker tree-border wall (lane cut through the bottom wall now), and the boomerang chest occasionally raced the script (poll). **Perfection**: run 3 = **34/34 beats pass** in 6½ min (`tools/regression.js`, recipe in the playtest skill). | 0 |
| 00:xx | Cycle 2 — gamepad | **Brainstorm**: gamepad / side quests / room polish / balance / world map. **Plan**: standard-mapping pad in `Player` (left stick or d-pad move, right stick aim else move direction else facing; A strike, B dash/sprint, X tool, Y potion, RB bomb, LB swap, Back bag, Start pause), `inputMode` in the store so hotbar/coach/journal hints switch to pad glyphs and back on any key/mouse. **Reality check**: no physical pad here — verified the plugin is on, keyboard unaffected, hint switching, the move lesson completes from the stick; the user should try a real pad once (Phaser 4 `pad.leftStick`/`buttons[i].pressed` typed fine). **Action**: shipped. **Perfection**: pause menu lists the pad layout. | 0 |
| 01:xx | Cycle 3 — side quests | **Brainstorm**: side quests / room polish / balance / world map / shrine save confirmation. **Plan**: `SIDE_QUESTS` in quests.ts (giver, counter, need, availability, reward); saved `counters` in the store; NPC bump offers (NEW OBJECTIVE plate) and turns in (thanks + gold/item) before the stall opens; tallies from the world with a running tag ("cap 2/4"); tracker shows the live side quest under the main objective ("· return" when ready); journal gets a SIDE QUESTS section (? offer, ▶ active, ! ready, ✓ done). Quests: Maren's Caps (4 mushrooms), What the Wardens Wrote (3 signs in the Hollow, after shard 1), Orrin's Slag (5 cinderlings, after shard 2). **Reality check**: the Hollow has only 2 mushrooms and cleared rooms don't respawn — mushrooms are plants now and regrow in cleared rooms. **Action**: shipped; verified by script (offer → tally across two visits → turn-in pays 60 gold + potion → journal ✓, no overlaps). **Perfection**: shops still open on the next bump; side tallies only count while the quest is on. | 0 |
| 01:xx | Cycle 4 — balance telemetry + a real bug | **Brainstorm**: balance from numbers / room polish / world map / shrine confirmation. **Plan**: instrument the regression with hearts lost and seconds per beat, run it, tune outliers. **Reality check**: the bot stands still in root fields and next to slimes, so its damage numbers overstate a human's — use them for order-of-magnitude only. **Action**: telemetry in `tools/regression.js` (`heartsLost`, `seconds` per beat, `console.table`). Numbers: Treant 19 half-hearts lost over 57 s at tier 1 (bot never dodges roots), Knight 7 over 20 s, Golem 13 over 13 s (stomp ring at point-blank), rooms 0–5 — nothing lopsided enough to tune blind; left as is. The run surfaced a **real bug**: after shard 1, Tam's bump was captured by the new side-quest offer, so `talked:elder:1` never set and the main quest stalled — main-quest lines now take priority over side offers (`mainPending`). **Perfection**: NPC bumps retry, boss intros wait for the store, the cistern slime is fought from the shore, the plate beat retries. | 0 |
| 02:xx | Cycle 5 — title chapter + shrine, regression green | **Brainstorm**: title/shrine polish / world map / chest flourish / boss-bar frames on the title. **Plan**: the Continue card says which chapter the save is on (title + objective; "The Ember is whole" after the finale, and the tagline changes), the shrine says "Saved." out loud. **Reality check**: `quitToTitle()` writes the live state, so seeding a save then quitting shows the live state — test the card from a real save. **Action**: shipped. **Perfection**: full regression after cycle 4's fix: 34/34. | 0 |
| 02:xx | Cycle 6 — dungeon map in the journal | **Brainstorm**: world map / chest flourish / vents art / hub tree border. **Plan**: a readable dungeon map at the top of the journal (M): room names on rooms you've seen, "?" on the rest, the current room lit, the boss hall outlined, cleared/solved rooms dimmed, the objective's room marked, doorways between seen rooms — same data as the minimap. **Reality check**: the journal gets tall — it scrolls (thin scrollbar). **Action**: shipped; verified in the crypt (3 seen, 1 here, 6 unknown, marker on the vault). **Perfection**: objective plates no longer fire while the journal/bag/pause is open. | 0 |
| 03:xx | Cycle 7 — Wren atlas | **Brainstorm**: chest flourish / vent art / tree border / load-time. **Plan**: 390 PNG requests on load, ~212 of them Wren frames — pack them into one atlas (`tools/pack_wren.py`, Phaser JSON-hash, 1020² 307 KB) and load it with `load.atlas`; anims reference atlas frames. **Reality check**: every frame is already on the 68×68 canvas, so origins/feet don't move; the atlas is rebuilt whenever a clip is fetched (skill updated). **Action**: shipped; verified every clip resolves (walk 6, attack 9, death 6, hurt 4, roll 6, idle 4 per direction) and the network shows 2 Wren requests instead of 212. **Perfection**: props (71 PNGs) could get the same treatment later. | 0 |
| 03:xx | Cycle 8 — props atlas | **Brainstorm**: props atlas / chest flourish / vent art. **Plan**: pack the 71 prop PNGs (`tools/pack_props.py`, shelf-packed 1024×266, 297 KB) and register each frame as its own texture key (`src/game/propAtlas.ts`, base frame = a window onto the atlas) so no call site changes. **Reality check**: per-file fallbacks can't be decided in preload (the atlas isn't loaded yet) — trust the packer, and the skill says to rerun it. **Action**: shipped; zero per-prop requests, everything renders. **Perfection**: regression 34/34 with both atlases; `openChests` now waits for the reveal (two chest beats had raced). | 0 |
| 04:xx | Cycle 9 — live check + chest flourish | **Brainstorm**: production sanity / chest feel / vent art / tree border. **Plan**: confirm the deployed site serves the atlases and voice clips (all 200), then give chests a moment: a beat of hit-stop, a shaft of light, a sparkle burst (gold fountains more, in gold), on top of the lid pop and the rising item. **Reality check**: keep it under half a second so chest-heavy rooms don't drag. **Action**: shipped; frame captured (`docs/mockup/frames/chest-flourish.png`). **Perfection**: nothing more — the flourish reads at 1x. | 0 |
| 04:xx | Cycle 10 — Cinder Depths sound pass | **Brainstorm**: cinder sounds (the third dungeon was borrowing whoosh/crystal/spore/bomb) / vent art / tree border / boss-hall dressing / regression pass. **Plan**: nine sounds in `audio.ts` with a new `crackle()` fire helper — rod shot, brazier ignite, vent breath, thorns burn, lava crusting (replaces the water gurgle in the cinder), magma splash, golem stomp, overheat sizzle, cinderling heat — wired at each event. **Reality check**: vents repeat every 3 s, so the hiss stays around 0.1 peak and only within 160 px; `heat` only fires when the cinderling wasn't already hot. **Action**: shipped; levels checked against the existing table (preview `docs/audio-review/sfx-cinder.wav`), regression 34/34 with the sound code exercised (1.9k oscillators over the run, no errors). **Perfection**: the stomp was quieter than the bomb it replaced — raised; audio skill gained the fire helper, the cinder set and the reference peaks. | 0 |
| 05:xx | Cycle 11 — lava pools light the room | **Brainstorm**: a cinder contact sheet first (`docs/sprite-review/qa-cinder.png`) — it showed the lava as flat orange sheets cut hard against the floor; other candidates: vent art, tree border, boss-hall dressing. **Plan**: banks and light with no new art — a 4 px scorched bank on the floor around every pool (inner corners handled), a 1 px ember lip just inside the edge, and additive halos under the pool's edges drawn *beneath* the lava sprite so only the stone catches the glow (the pool keeps its own colour), breathing out of step. When a plate crusts the pool, the lip and glow fade and the dark bank stays around the slag. **Reality check**: halos over the lava blew it out — hence under it; the crypt's pale water rim is unchanged (cistern: 16 rims, 0 glow). **Action**: shipped; review `docs/sprite-review/lava-banks.png` (Brazier Vault lit, Thorn Gate crusted). **Perfection**: first pass was too faint (0.16 α at tile centres) — moved to the edges at 0.55–0.85. Playtest note for the skill: `__snap` only resolves after a render, so on a hidden pane call it, then `__run(120)`, then await it. | 0 |
| 05:xx | Cycle 12 — water gets the same banks | **Brainstorm**: crypt water (the cistern read as a floor stain next to the new lava) / vent glow / boss-hall dressing / Whisperwood re-check. **Plan**: the lava code now serves both liquids — a 4 px wet-stone bank on the floor around every pool, a pale foam lip just inside that laps slowly (α 0.5↔0.2, 1.6 s), no glow; the lip fades with the water when a plate drains it and the dark basin edge stays. **Reality check**: the old 2 px pale rim was doing nothing at 1x; the drain path had to kill the lap tween before fading (checked: rims → 0, pool gone). **Action**: shipped; review `docs/sprite-review/water-banks.png`. **Perfection**: nothing further — same helper, one branch per liquid. | 0 |

